# ... Service YAML definition
apiVersion: v1
kind: Service
metadata:
  name: pauta-api
spec:
  selector:
    app: pauta-api
  ports:
    - port: 80
      targetPort: 8080
      #nodePort: 30000
  type: LoadBalancer
