# ... Deployment YAML definition
apiVersion: apps/v1
kind: Deployment
metadata:
  name: pauta-api
spec:
  replicas: 1
  selector:
    matchLabels:
      app: pauta-api
  template:
    metadata:
      labels:
        app: pauta-api
    spec:
      containers:
        - name: app
          image: davilopess/pauta-api:1.0.2
          ports:
            - containerPort: 8080
          env:
            - name: MONGO_URL
              value: mongodb+srv://davilopesamorim56:LtOWFJFVnMpbshdH@pauta.2gdia.mongodb.net/?retryWrites=true&w=majority&appName=Pauta
          imagePullPolicy: Always


